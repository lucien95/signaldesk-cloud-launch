[tool.pytest.ini_options]
pythonpath = ["app"]
testpaths = ["app/tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["B008"] # FastAPI's dependency-injection pattern uses Depends in defaults.
